# Test Environment Configuration
DATABASE_URL="postgresql://postgres:.@localhost:5432/whatsapp_bot_test"

# JWT & Authentication
JWT_SECRET="test-jwt-secret-key-minimum-32-characters-for-testing-purposes-only"
JWT_EXPIRES_IN="15m"
REFRESH_TOKEN_SECRET="test-refresh-token-secret-different-from-jwt-secret-for-testing"
REFRESH_TOKEN_EXPIRES_IN="7d"

# Application
APP_URL="http://localhost:3000"
PORT=3001
ALLOWED_ORIGINS="http://localhost:3000"
NODE_ENV="test"

# Redis (optional for tests)
REDIS_URL="redis://localhost:6379"
REDIS_PASSWORD=""

# RabbitMQ (optional for tests)
RABBITMQ_URL="amqp://admin:admin@localhost:5671"

# Mail (disabled for tests)
MAIL_HOST="smtp.example.com"
MAIL_PORT=587
MAIL_SECURE="false"
MAIL_USER="test@example.com"
MAIL_PASS="test-password"
MAIL_FROM="Test <test@example.com>"
